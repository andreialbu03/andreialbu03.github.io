// next.config.mjs
const nextConfig = {
  // Export static HTML with `next export`
  output: "export",
  // basePath: "/<repo>", // uncomment if deploying to username.github.io/<repo>
  // assetPrefix: "/<repo>/",
};
export default nextConfig;
